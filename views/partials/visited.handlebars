<div class="">
    <div class="m-2 p-2 bg-success">
        <h3>Parks Visited</h3>
    </div>
    <div class="m-2 p-2 bg-success">
        <ul>
            {{#each visited as |park|}}
            <li>{{park.park_name}}</li>
            {{/each}}
        </ul>
    </div>
</div>